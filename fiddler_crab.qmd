---
title: "fiddler crab"
format: html
editor: visual
---

```{r}
library(lterdatasampler)
library(tidyverse)
```

Creating exploratory plot

```{r}
pie_crab |> 
  ggplot(aes(x = size, y = latitude)) +
  geom_point()
```
